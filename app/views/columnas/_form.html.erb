<%= form_for(@columna) do |f| %>
  <% if @columna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@columna.errors.count, "error") %> prohibited this columna from being saved:</h2>

      <ul>
      <% @columna.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
